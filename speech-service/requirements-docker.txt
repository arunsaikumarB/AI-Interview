# CPU-friendly image deps (no nvidia-* wheels — use host GPU via compose profile if needed)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
faster-whisper>=1.1.0
numpy>=1.26.0
piper-tts>=1.2.0
ctranslate2>=4.0.0
av>=12.0.0
onnxruntime>=1.17.0
