/** @deprecated Prefer `@/lib/ai/ollama`. Kept for temporary imports. */
export {
  AIError,
  chatJSON,
  embed,
  getAIProvider,
  healthCheck,
  healthCheck as ollamaHealth,
  getOllamaConfig,
} from "@/lib/ai/ollama";
